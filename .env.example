# 豆包API配置
ARK_API_KEY=your_api_key_here

# ========================================
# 并发性能优化配置 (快慢双车道策略)
# ========================================
# 
# 智能双车道系统：
# - 快车道 (DeepSeek, Doubao Pro等): RPM=30000 → 500并发
# - 慢车道 (seed-translation): RPM=5000 → 80并发
# 
# 系统会根据模型自动选择车道，无需手动配置！

# 最大并发请求数 (这个参数主要影响非seed-translation模型)
# 推荐值: 500 (快车道模型的理论上限)
# seed-translation 模型会自动限制在80并发
MAX_CONCURRENT_REQUESTS=500

# 每分钟最大请求数 (自动计算RPS)
# 推荐值: 
#   - 快车道: 30000 (接近RPM=30000的限制)
#   - 慢车道: 自动限制为4800 (略低于RPM=5000)
REQUESTS_PER_MINUTE=30000

# 或者直接设置每秒请求数 (二选一，REQUESTS_PER_MINUTE优先)
# MAX_REQUESTS_PER_SECOND=500.0

# ========================================
# 翻译语言配置
# ========================================
# 源语言 (留空表示自动检测)
SOURCE_LANGUAGE=

# 目标语言 (默认中文简体)
# 支持: zh, zh-Hant, en, ja, ko, fr, de, es, it, pt, ru, ar, th, vi
TARGET_LANGUAGE=zh

# ========================================
# 高级配置（可选）
# ========================================
# 模型列表（逗号分隔，优先使用第一个）
# 快车道模型: deepseek-v3-250324, doubao-seed-1-6-251015, doubao-1-5-vision-pro-32k-250115
# 慢车道模型: doubao-seed-translation-250915
# ARK_MODELS=doubao-seed-translation-250915,deepseek-v3-250324

# ========================================
# 性能调优建议
# ========================================
# 
# 场景1: 使用快车道模型 (DeepSeek, Doubao Pro)
# - MAX_CONCURRENT_REQUESTS=500
# - REQUESTS_PER_MINUTE=30000
# - 预期吞吐: ~500 req/sec
# - 适合: 大量文本翻译、高并发场景
#
# 场景2: 使用慢车道模型 (seed-translation)
# - 系统自动限制为80并发
# - 无需手动配置
# - 预期吞吐: ~80 req/sec
# - 适合: 免费额度使用、稳定翻译
#
# 场景3: 混合使用 (推荐)
# - models.json 中同时配置快慢车道模型
# - 长文本自动切换到快车道
# - 短文本使用慢车道节省成本
#

# ========================================
# 监控指标说明
# ========================================
# 如果频繁出现以下情况，需要调整配置:
# 
# 1. 429错误（Too Many Requests）
#    → 降低 MAX_CONCURRENT_REQUESTS 或 REQUESTS_PER_MINUTE
#
# 2. 超时错误（Timeout）
#    → 降低 MAX_CONCURRENT_REQUESTS
#
# 3. TPM超限（Token配额用尽）
#    → 降低并发或限制单次请求的文本长度
#
# 4. 响应过慢
#    → 检查网络，或降低并发数
#
# ========================================
# 支持的模型列表 (参考models.json)
# ========================================
# 
# 快车道模型 (RPM=30000, 500并发):
# - deepseek-v3-250324
# - deepseek-v3-1-terminus  
# - doubao-seed-1-6-lite-251015
# - doubao-seed-1-6-251015
# - doubao-1-5-vision-pro-32k-250115
# - doubao-1.5-vision-pro-250328
# - doubao-1.5-vision-lite-250315
# - doubao-1-5-ui-tars-250428
#
# 慢车道模型 (RPM=5000, 80并发):
# - doubao-seed-translation-250915
#